import type React from 'react';

export const Home: React.FC = () => {
  return (
    <main>
      <h1>Zine Layout</h1>
      <p>Prototype UI. Use the health indicator to check backend status.</p>
      <ul>
        <li>React + RTK Toolkit scaffolding</li>
        <li>Build with Vite; bundled with Dagger</li>
        <li>Next steps: projects CRUD, image upload, spec editing</li>
      </ul>
    </main>
  );
};
